version: '3.8'

services:
  web:
    build: .
    container_name: f1-stats-hub
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - AUTH_TRUST_HOST=true
    restart: unless-stopped
